<?xml version="1.0"?>
<parameters>

    <!-- Connection details -->
    <type>CUBRID</type>
    <driver>cubrid.jdbc.driver.CUBRIDDriver</driver>
    <url>jdbc:cubrid:localhost:33000:sibench:::</url>
    <username>dba</username>
    <password></password>
    <reconnectOnConnectionFailure>true</reconnectOnConnectionFailure>
    <isolation>TRANSACTION_READ_COMMITTED</isolation>
    <batchsize>128</batchsize>

    <scalefactor>1</scalefactor>

    <!-- The workload -->
    <terminals>1</terminals>
    <works>
        <work>
            <time>300</time>
            <rate>unlimited</rate>
            <weights>50,50</weights>
        </work>
    </works>

    <!-- SIBENCH Procedures declaration -->
    <transactiontypes>
        <transactiontype>
            <name>MinRecord</name>
        </transactiontype>
        <transactiontype>
            <name>UpdateRecord</name>
        </transactiontype>
    </transactiontypes>
</parameters>
