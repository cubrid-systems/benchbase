<?xml version="1.0"?>
<dialects>
    <dialect type="CUBRID">
        <procedure name="Q1">
            <statement name="query_stmt">
                SELECT
                   l_returnflag,
                   l_linestatus,
                   SUM(l_quantity) AS sum_qty,
                   SUM(l_extendedprice) AS sum_base_price,
                   SUM(l_extendedprice * (1 - l_discount)) AS sum_disc_price,
                   SUM(l_extendedprice * (1 - l_discount) * (1 + l_tax)) AS sum_charge,
                   AVG(l_quantity) AS avg_qty,
                   AVG(l_extendedprice) AS avg_price,
                   AVG(l_discount) AS avg_disc,
                   COUNT(*) AS count_order
                FROM
                   lineitem
                WHERE
                   l_shipdate &lt;= DATE_SUB(DATE '1998-12-01', INTERVAL ? DAY)
                GROUP BY
                   l_returnflag,
                   l_linestatus
                ORDER BY
                   l_returnflag,
                   l_linestatus
            </statement>
        </procedure>
        <procedure name="Q3">
            <statement name="query_stmt">
                SELECT
                   l_orderkey,
                   SUM(l_extendedprice * (1 - l_discount)) AS revenue,
                   o_orderdate,
                   o_shippriority
                FROM
                   customer,
                   orders,
                   lineitem
                WHERE
                   c_mktsegment = ?
                   AND c_custkey = o_custkey
                   AND l_orderkey = o_orderkey
                   AND o_orderdate &lt; ?
                   AND l_shipdate > ?
                GROUP BY
                   l_orderkey,
                   o_orderdate,
                   o_shippriority
                ORDER BY
                   revenue DESC,
                   o_orderdate LIMIT 10
            </statement>
        </procedure>
        <procedure name="Q4">
            <statement name="query_stmt">
                SELECT
                   o_orderpriority,
                   COUNT(*) AS order_count
                FROM
                   orders
                WHERE
                   o_orderdate >= ?
                   AND o_orderdate &lt; DATE_ADD(?, INTERVAL 3 MONTH)
                   AND EXISTS
                   (
                      SELECT
                         *
                      FROM
                         lineitem
                      WHERE
                         l_orderkey = o_orderkey
                         AND l_commitdate &lt; l_receiptdate
                   )
                GROUP BY
                   o_orderpriority
                ORDER BY
                   o_orderpriority
            </statement>
        </procedure>
        <procedure name="Q5">
            <statement name="query_stmt">
                SELECT
                   n_name,
                   SUM(l_extendedprice * (1 - l_discount)) AS revenue
                FROM
                   customer,
                   orders,
                   lineitem,
                   supplier,
                   nation,
                   region
                WHERE
                   c_custkey = o_custkey
                   AND l_orderkey = o_orderkey
                   AND l_suppkey = s_suppkey
                   AND c_nationkey = s_nationkey
                   AND s_nationkey = n_nationkey
                   AND n_regionkey = r_regionkey
                   AND r_name = ?
                   AND o_orderdate >= ?
                   AND o_orderdate &lt; DATE_ADD(?, INTERVAL 1 YEAR)
                GROUP BY
                   n_name
                ORDER BY
                   revenue DESC
            </statement>
        </procedure>
        <procedure name="Q6">
            <statement name="query_stmt">
                SELECT
                   SUM(l_extendedprice * l_discount) AS revenue
                FROM
                   lineitem
                WHERE
                   l_shipdate >= ?
                   AND l_shipdate &lt; DATE_ADD(?, INTERVAL 1 YEAR)
                   AND l_discount BETWEEN ? - 0.01 AND ? + 0.01
                   AND l_quantity &lt; ?
            </statement>
        </procedure>
        <procedure name="Q10">
            <statement name="query_stmt">
                SELECT
                   c_custkey,
                   c_name,
                   SUM(l_extendedprice * (1 - l_discount)) AS revenue,
                   c_acctbal,
                   n_name,
                   c_address,
                   c_phone,
                   c_comment
                FROM
                   customer,
                   orders,
                   lineitem,
                   nation
                WHERE
                   c_custkey = o_custkey
                   AND l_orderkey = o_orderkey
                   AND o_orderdate >= ?
                   AND o_orderdate &lt; DATE_ADD(?, INTERVAL 3 MONTH)
                   AND l_returnflag = 'R'
                   AND c_nationkey = n_nationkey
                GROUP BY
                   c_custkey,
                   c_name,
                   c_acctbal,
                   c_phone,
                   n_name,
                   c_address,
                   c_comment
                ORDER BY
                   revenue DESC LIMIT 20
            </statement>
        </procedure>
        <procedure name="Q11">
            <statement name="query_stmt">
                SELECT
                   ps_partkey,
                   SUM(ps_supplycost * ps_availqty) AS total_value
                FROM
                   partsupp,
                   supplier,
                   nation
                WHERE
                   ps_suppkey = s_suppkey
                   AND s_nationkey = n_nationkey
                   AND n_name = ?
                GROUP BY
                   ps_partkey
                HAVING
                   SUM(ps_supplycost * ps_availqty) > (
                   SELECT
                      SUM(ps_supplycost * ps_availqty) * ?
                   FROM
                      partsupp, supplier, nation
                   WHERE
                      ps_suppkey = s_suppkey
                      AND s_nationkey = n_nationkey
                      AND n_name = ? )
                   ORDER BY
                      total_value DESC
            </statement>
        </procedure>
        <procedure name="Q12">
            <statement name="query_stmt">
                SELECT
                    l_shipmode,
                    SUM(
                        CASE
                            WHEN
                                o_orderpriority = '1-URGENT' OR o_orderpriority = '2-HIGH'
                            THEN
                                1
                            ELSE
                                0
                        END
                    ) AS high_line_count,
                    SUM(
                        CASE
                            WHEN
                                o_orderpriority &lt;> '1-URGENT' AND o_orderpriority &lt;> '2-HIGH'
                            THEN
                                1
                            ELSE
                                0
                        END
                    ) AS low_line_count
                FROM
                    orders,
                    lineitem
                WHERE
                    o_orderkey = l_orderkey
                    AND l_shipmode IN (?, ?)
                    AND l_commitdate &lt; l_receiptdate
                    AND l_shipdate &lt; l_commitdate
                    AND l_receiptdate >= ?
                    AND l_receiptdate &lt; DATE_ADD(?, INTERVAL 1 YEAR)
                GROUP BY
                    l_shipmode
                ORDER BY
                    l_shipmode
            </statement>
        </procedure>
        <procedure name="Q14">
            <statement name="query_stmt">
                SELECT
                   100.00 * SUM(
                   CASE
                      WHEN
                         p_type LIKE 'PROMO%'
                      THEN
                         l_extendedprice * (1 - l_discount)
                      ELSE
                         0
                   END
                ) / SUM(l_extendedprice * (1 - l_discount)) AS promo_revenue
                FROM
                   lineitem, part
                WHERE
                   l_partkey = p_partkey
                   AND l_shipdate >= ?
                   AND l_shipdate &lt; DATE_ADD(?, INTERVAL 1 MONTH)
            </statement>
        </procedure>
        <procedure name="Q15">
            <statement name="createview_stmt">
                CREATE view revenue0 (supplier_no, total_revenue) AS
                SELECT
                   l_suppkey,
                   SUM(l_extendedprice * (1 - l_discount))
                FROM
                   lineitem
                WHERE
                   l_shipdate >= ?
                   AND l_shipdate &lt; DATE_ADD(?, INTERVAL 3 MONTH)
                GROUP BY
                   l_suppkey
            </statement>
        </procedure>
        <procedure name="Q20">
            <statement name="query_stmt">
                SELECT
                   s_name,
                   s_address
                FROM
                   supplier,
                   nation
                WHERE
                   s_suppkey IN
                   (
                      SELECT
                         ps_suppkey
                      FROM
                         partsupp
                      WHERE
                         ps_partkey IN
                         (
                            SELECT
                               p_partkey
                            FROM
                               part
                            WHERE
                               p_name LIKE ?
                         )
                         AND ps_availqty > (
                         SELECT
                            0.5 * SUM(l_quantity)
                         FROM
                            lineitem
                         WHERE
                            l_partkey = ps_partkey
                            AND l_suppkey = ps_suppkey
                            AND l_shipdate >= ?
                            AND l_shipdate &lt; DATE_ADD(?, INTERVAL 1 YEAR) )
                   )
                   AND s_nationkey = n_nationkey
                   AND n_name = ?
                ORDER BY
                   s_name
            </statement>
        </procedure>
    </dialect>
</dialects>
